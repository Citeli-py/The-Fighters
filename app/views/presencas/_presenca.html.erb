<div id="<%= dom_id presenca %>" class="card">
  <strong><%= presenca.aluno.nome %></strong>

  <%= button_to "Remover",
    aula_presenca_path(presenca.aula, presenca),
    method: :delete,
    class: "btn btn-danger btn-sm",
    data: { turbo_confirm: "Remover presença?" } %>
</div>
